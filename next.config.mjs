/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en', 'es', 'de'], // Lista de idiomas soportados
        defaultLocale: 'en', // Idioma por defecto
      }
};

export default nextConfig;
